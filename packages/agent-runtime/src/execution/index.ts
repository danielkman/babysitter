/**
 * Execution module — execution mode abstraction interfaces.
 */
export type {
  ExecutionMode,
  LocalExecutionConfig,
  DockerExecutionConfig,
  SshExecutionConfig,
  KubernetesExecutionConfig,
  ExecutionConfig,
  ExecutionHandle,
  ExecutionProvider,
} from "./types";
