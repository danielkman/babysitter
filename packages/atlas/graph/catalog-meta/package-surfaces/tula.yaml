nodeKind: PackageSurface
id: package:a5c-ai-tula
attributes:
  packageId: "@a5c-ai/tula"
  packageName: "@a5c-ai/tula"
  workspacePath: packages/tula
  moduleType: typescript
  surfaceKinds:
    - agent-runtime
    - bin-entrypoint
    - cli
    - llm-client
    - provider-facade
  sourceOfTruthRole: application
  publishedTo: npm
  versionPolicy: semver
  # Source: packages/tula/package.json (exports ./cli barrel + root barrel).
  # Bin: tula. Composes agent-core, agent-runtime, agent-platform, adapters,
  # and babysitter-sdk into a single installable product.
edges:
  references_path:
    - target: path:a5c-runs-dir
  exposes_subcommand:
    - target: interaction-primitive:babysitter-sdk-cli-harness-runtime
