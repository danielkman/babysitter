/**
 * Re-export shim — install logic now lives in @a5c-ai/agent-mux-config.
 */
export {
  installCommand,
  INSTALL_FLAGS,
  type SpawnRunner,
  type InstallCommandDeps,
} from '@a5c-ai/agent-mux-config';
