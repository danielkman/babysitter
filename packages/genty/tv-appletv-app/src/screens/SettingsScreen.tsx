import React from 'react';
import { View } from 'react-native';

import { Text } from '@a5c-ai/genty-ui';

export function SettingsScreen(): JSX.Element {
  return (
    <View>
      <Text>Settings: opt-in hook approval and pairing diagnostics</Text>
    </View>
  );
}
